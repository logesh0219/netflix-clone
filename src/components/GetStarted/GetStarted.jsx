import React from 'react';
import './GetStarted.css';
import EmailPrompt from '../../Common/EmailPrompt/EmailPrompt';

const GetStarted = ({emailPara}) => {
  return <EmailPrompt  emailPara={emailPara}/>
};

export default GetStarted;
